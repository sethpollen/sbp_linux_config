TODO: fix Ctrl+Shift+I shortcut in kate so it invokes the GoFmt tool. This
mapping gets stored in an XML file somewhere under $HOME with "external" and
"ui" in the path.

TODO: Bazel rules for converting openscad to stl. Something like this:
  openscad --export-format binstl -o test.stl -D print_quality=false print1.scad
