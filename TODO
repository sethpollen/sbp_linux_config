1. Get rid of hosts/. Instead, each machine will have its own script that runs
   the generic makefile and then copies in its own specific extensions.
2. Get rid of make-links.sh. The generic makefile should do this stuff.
