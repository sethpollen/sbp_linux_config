# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks

# The global properties below are applied to every block, but can be overridden.
separator_block_width=5
markup=none

[backlight]
command=.config/i3blocks/backlight | format-percent --label=✻ | i3blocks-pad
interval=1

# The volume indicator responds to your touch!
#   Right-click: Toggle mute
#   Scroll up: Volume up
#   Scroll down: Volume down
[volume]
command=.config/i3blocks/volume | format-percent --label=♪ | i3blocks-pad
instance=Master
interval=1

[disk]
command=/usr/share/i3blocks/disk | i3blocks-pad
interval=10
color=#9F9F9F

[network]
command=.config/i3blocks/network | i3blocks-pad
interval=2

[cpu_usage]
# -w 101 -c 101 disables the yellow/red warning colors.
command=/usr/share/i3blocks/cpu_usage -w 101 -c 101 | format-percent --keep_number=1 --history=10 --history_id=cpu_usage | i3blocks-pad
# The cpu_usage script blocks for 1 second to collect stats, so we just call
# it in a loop.
interval=repeat

[battery]
command=.config/i3blocks/battery | format-percent --label=⚡ | i3blocks-pad
interval=5

# 'persist' works on i3blocks 1.4, but it doesn't accept clicks (unlike the HEAD
# release). See https://github.com/vivien/i3blocks/pull/306. So to make
# http://go/i3cal to work, we will need to do some more work.

[time]
command=date '+ %a %m-%d %l:%M %p '
interval=10
color=#FF9900
