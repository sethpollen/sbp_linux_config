# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# TODO: delete old unused python files

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/share/i3blocks/$BLOCK_NAME
separator_block_width=15
markup=none
# TODO: The bar has too much space if the blocklet ends with a bar character

[backlight]
command=.config/i3blocks/backlight | percent2bar --label=✻ 
interval=1
# TODO: rip off the cool volume responsiveness
# TODO: this looks weird on costaguana

# The volume indicator responds to your touch!
#   Right-click: Toggle mute
#   Scroll up: Volume up
#   Scroll down: Volume down
[volume]
command=/usr/share/i3blocks/volume | percent2bar --label=♪
instance=Master
interval=1
# TODO: better formatting of MUTE
# TODO: nix the space after the label

[disk]
interval=10
color=#9F9F9F

[iface]
color=#00FF00
interval=2
separator=false
# TODO: just show E(green)/W(green)/N(red) for wired, wireless, none

[wifi]
interval=2
separator=false
# TODO: I think this is supposed to show wifi network ID and signal quality
# Seems like i need to pass the interface name (cf. /proc/net/wireless)
# Though somehow the other blocks find the right interface...

[bandwidth]
interval=2
# TODO: use up/down arrows
# TODO: use a bar for signal quality
# TODO: use my 3-char KiB formatting

# The cpu_usage script blocks for 1 second to collect stats, so we just call
# it in a loop.
[cpu_usage]
command=/usr/share/i3blocks/cpu_usage | percent2bar --keep_number=1 --history=10 --history_id=cpu_usage
interval=repeat
# TODO: add my history graph
# TODO: compress the percentage readout
# TODO: make the script block for 2 seconds
# TODO: when clicked, open a terminator with htop
# TODO: don't turn yellow under high load

[battery]
command=/usr/share/i3blocks/battery | percent2bar --label=⚡
interval=10
# TODO: replace CHR/DIS with up/down arrows


[time]
command=date '+%m-%d %l:%M %p'
interval=10
color=#FFFF00

# TODO: differentiate blocks with colors
