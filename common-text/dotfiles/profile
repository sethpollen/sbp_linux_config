# TODO: need to adapt for fish somehow

# Environment variables set here will be available to all shells.
# In particular, this sets up the environment for an SSH session.

# Source standard environment variables.
tmp="$(mktemp)"
~/bin/sbp-environment > "$tmp"
. "$tmp"
rm "$tmp"
