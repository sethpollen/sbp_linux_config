Subdirectories:
  python - contains Python libraries used by other scripts in sbp-linux-config
  scripts - contains raw executable scripts (mostly in bash or python)
  dotfiles - contains dotfiles to be installed to $HOME
  wallpaper - contains my favorite desktop wallpapers
  code.google.com/p/sbp-linux-config - contains Go source
